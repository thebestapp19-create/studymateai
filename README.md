# StudyMate AI

React + TypeScript + Vite + Tailwind CSS.

## Development

```bash
npm install
npm run dev
```

## Current scope

Only the first onboarding screen is implemented: it collects the user's name
and grade and stores them in `localStorage` under `studymate.userProfile`
(see `src/lib/userProfile.ts`).
